diff --git a/Firmware/Tests/test_as5600.cpp b/Firmware/Tests/test_as5600.cpp
index a5b254d..57ac861 100644
--- a/Firmware/Tests/test_as5600.cpp
+++ b/Firmware/Tests/test_as5600.cpp
@@ -133,9 +133,11 @@ TEST_SUITE("AS5600 helpers") {
 
     TEST_CASE("plausibility threshold has an absolute anti-glitch cap") {
         CHECK(as5600::max_plausible_raw_delta(
-                  as5600::STALE_TICKS, 1.0f / 8000.0f, 20.0f) == 192);
+                  as5600::STALE_TICKS, 1.0f / 8000.0f, 20.0f) == 209);
         CHECK(as5600::max_plausible_raw_delta(
-                  UINT32_MAX, 1.0f / 8000.0f, 30.0f) == 192);
+                  as5600::STALE_TICKS, 1.0f / 8000.0f, 30.0f) == 312);
+        CHECK(as5600::max_plausible_raw_delta(
+                  UINT32_MAX, 1.0f / 8000.0f, 30.0f) == 320);
     }
 
     TEST_CASE("zero crossing remains plausible") {
@@ -224,4 +226,29 @@ TEST_SUITE("AS5600 helpers") {
         CHECK(!as5600::recovery_attempt_due(state, 0xfffffffeu));
         CHECK(as5600::recovery_attempt_due(state, 2));
     }
+    TEST_CASE("recovery re-anchor accepts large legal motion and invalidates multi-turn") {
+        auto r = as5600::recovery_reanchor(5000, 1300, 1000);
+        CHECK(r.raw_delta == 300);
+        CHECK(r.resumed_shadow == 5300);
+        CHECK(!r.multiturn_valid);
+    }
+
+    TEST_CASE("recovery re-anchor crosses the AS5600 zero boundary") {
+        auto r = as5600::recovery_reanchor(8191, 2, 4095);
+        CHECK(r.raw_delta == 3);
+        CHECK(r.resumed_shadow == 8194);
+        CHECK(!r.multiturn_valid);
+    }
+
+    TEST_CASE("recovery movement beyond half a turn is explicitly ambiguous") {
+        // From 1000 to 3500 the AS5600 alone cannot distinguish +2500 counts
+        // from the nearer -1596-count path. Preserve the nearest path but mark
+        // multi-turn state invalid so an application cannot mistake it for an
+        // absolute multi-turn reconstruction.
+        auto r = as5600::recovery_reanchor(10000, 3500, 1000);
+        CHECK(r.raw_delta == -1596);
+        CHECK(r.resumed_shadow == 8404);
+        CHECK(!r.multiturn_valid);
+    }
+
 }
diff --git a/Firmware/odrive-interface.yaml b/Firmware/odrive-interface.yaml
index 10ba6aa..9cf8734 100644
--- a/Firmware/odrive-interface.yaml
+++ b/Firmware/odrive-interface.yaml
@@ -1335,6 +1335,15 @@ interfaces:
       rejected_sample_count:
         type: readonly uint32
         doc: Total AS5600 RAW ANGLE samples rejected as physically implausible since boot.
+      as5600_pending_skip_count:
+        type: readonly uint32
+        doc: Number of 8 kHz sampling slots skipped because the previous AS5600 interrupt transaction was still pending.
+      as5600_reanchor_count:
+        type: readonly uint32
+        doc: Number of AS5600 samples accepted as the first re-anchor sample after successful bus recovery.
+      as5600_multiturn_valid:
+        type: readonly bool
+        doc: True while the software multi-turn accumulator is unambiguous. It becomes false after I2C recovery because motion during the communication gap cannot be reconstructed from a single-turn AS5600.
       i2c_error_rate: readonly float32
       sample_age:
         type: readonly float32
@@ -1376,11 +1385,12 @@ interfaces:
             type: float32
             unit: turn/s
             default: 20.0
-            doc: Maximum physically plausible AS5600 mechanical speed (valid range above 0 through 30 turn/s). The firmware adds timing and quantization margin, with a 192-count absolute anti-glitch cap.
+            doc: Maximum physically plausible AS5600 mechanical speed (valid range above 0 through 30 turn/s). The firmware adds timing and quantization margin, with a 320-count absolute anti-glitch cap.
           as5600_calibration_version:
             type: uint8
+            c_setter: set_as5600_calibration_version
             default: 0
-            doc: Firmware writes 1 only after a successful AS5600 encoder offset calibration. Existing configurations must be recalibrated after upgrading; users must not set this field manually.
+            doc: Firmware writes 1 only after a successful AS5600 encoder offset calibration. External writes may only clear this field to 0; attempts to set a nonzero value are ignored. Existing configurations must be recalibrated after upgrading.
           pre_calibrated: {type: bool, c_setter: set_pre_calibrated}
           enable_phase_interpolation: bool
           bandwidth: 
